#include <cstdlib>

class MyClass {};
MyClass x;

// we'll get these values from the debugger and pass them to Type.array() to
// create array types of different lengths.
int int_bound = 10;
char char_bound = 20;
size_t size_t_bound = 30;
float float_bound = 40.1f;

int main() { return 0; }

